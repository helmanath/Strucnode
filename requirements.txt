# Core runtime
Pillow>=10.0.0
numpy>=1.24.0

# Optional: enables video, RAW decoding, EXIF and audio.
# Install with `pip install -e ".[media]"` or uncomment as needed.
# opencv-python>=4.8.0
# rawpy>=0.19.0
# exifread>=3.0.0
# sounddevice>=0.4.6
# moviepy>=1.0.3
